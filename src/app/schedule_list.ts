import { Schedule } from './schedule'

export const SCHEDULES: Schedule[] = [
    {name: 'default Schedule',
    courses:[{
        catalog_nbr:'default class',
        subject:'default subject'
}]}
]